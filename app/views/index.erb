<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div id="statistics">
      <% @statistics.each do |key, value| %>
      <% value = value.join(", ") if value.class == Array %>
      <p><%= "#{key.to_s}: #{value.to_s} "%></p>
      <% end %>
      <p>List of urls:
        <% @list_of_paths.each_with_index do |path, index| %>
        <a href="<%=url("/sources/#{identifier}/urls#{path}")%>"><%=@list_of_urls[index]%></a>
        <% end %>
      </p>
    </div>
  </body>
</html>
